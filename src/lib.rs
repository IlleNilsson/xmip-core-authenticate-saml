#![forbid(unsafe_code)]
//! Authenticate by saml: verifies an assertion's signature and conditions against the `IdP`
//! metadata.
//!
//! Declared and not yet written: `architecture.toml` carries the maturity. When it
//! is, it implements `Authenticator` (ADR-0050).
